# Ticket-Management-System
Created a ticket management system to track the issues raised. We can perform CRUD operations from the UI as well the DB. Also, created a audit log to capture the actions performed.
